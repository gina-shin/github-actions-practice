require('colors');
var fs = require('fs');
var compile = require('..');

var str = fs.readFileSync('test.bars', 'utf8');

console.log(str);

var tree = compile(str, 'test.bars', 'DOM', {
    verbose: true
});

fs.writeFileSync('test.json', JSON.stringify(tree, null, 2));
